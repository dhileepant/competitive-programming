class Solution:
    def triangularSum(self, nums: List[int]) -> int:
        while len(nums) != 1:
            n = len(nums)
            for i in range(n-1):
                nums[i] = (nums[i] + nums[i+1]) % 10
            nums.pop()
        return nums[0]
